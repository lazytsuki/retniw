# CODE-REVIEW

> 历史记录：本文件只对应PRD V2.0基线。当前V2.3零说明书体验、生命周期迁移与公开自注册请以`CODE-REVIEW-M007.md`为准；当前版本尚未完成复审。

## 1. 文档概览

- 文档状态：Final
- 输出文件：`CODE-REVIEW.md`
- 评审目标：对照 PRD V2.0、技术方案和六张任务卡，评估当前未提交实现的需求对齐、风险与验证充分性
- 总体 verdict：`有条件通过`
- 合入建议：`可作为 retniw 第二版本地基线；正式发布前执行上线检查清单`

## 2. 输入来源

### 2.1 需求侧证据

- `PRD.md`：连续思考、主动 AI、开放导入导出、独立关系和跨端验收口径。
- `TECH-DESIGN.md`：数据结构、服务端边界、离线、流式输出和验证映射。
- `TASK-STATUS.md` 与 `TASK-M-001` 至 `TASK-M-006`：实施范围、依赖和完成定义。

### 2.2 变更侧证据

- 对比基线：当前 `HEAD` 与未提交工作区。
- 变更范围：Vite 原型迁移为 Next.js App Router，新增 Supabase 迁移、服务端仓储、API、响应式工作区、PWA、测试与真实链路验证脚本。
- 变更检查：`git diff --check` 通过，未发现空白符或冲突标记问题。

### 2.3 本次评审范围

- 包含：登录后的记录、续写、主动 AI、关系、导入导出、离线恢复、手机/Mac 适配、权限隔离与配置边界。
- 不包含：Vercel 正式部署、自定义域名、线上监控和真实手机桌面安装。

## 3. 评审结论

### 3.1 Verdict 摘要

- 阻塞问题：0
- 重要风险：0
- 一般建议：0
- 待确认问题：1

### 3.2 合入建议

- 当前实现可作为第二版本地基线。正式发布应在 Vercel 注入环境变量，按顺序执行数据迁移与应用部署，再重跑主链路。

### 3.3 非评审范围

- 语音转写已明确延后，本轮不评估。
- 不根据当前雏形扩展任务、聊天机器、向量库或图谱编辑器。

## 4. 需求-变更追溯矩阵

| ID | 需求/约束 | 变更证据 | 结论 | 备注 |
|---|---|---|---|---|
| TR-001 | 连续记录，不强制拆成对话 | `ThoughtWorkspace`、`EntryRepository`、连续追加测试 | 已覆盖 | 输入始终可用 |
| TR-002 | AI 仅由用户主动调用 | AI SSE 路由、上下文门槛、三种操作测试 | 已覆盖 | 普通输入不触发 AI；AI 输出不作为用户事实继续扩写 |
| TR-003 | 导入与导出保持开放 | 文本解析、Markdown/JSON 流式路由、4.5MB 以上验证 | 已覆盖 | JSON 版本为 `retniw.export.v1` |
| TR-004 | 关系检查独立且不复活 | 关系仓储、唯一约束、并发与状态测试 | 已覆盖 | 忽略或确认后不再提出 |
| TR-005 | 手机和 Mac 同功能 | 响应式 CSS、手机/Mac 浏览器验收 | 已覆盖 | 900px 以下单列 |
| TR-006 | 失败不丢内容 | IndexedDB outbox、离线页、重试与草稿恢复 | 已覆盖 | 离线重试后当页保留 |
| TR-007 | 个人数据不越权、密钥不进浏览器 | Cookie 会话、服务端 userId 过滤、RLS、跨账号验证 | 已覆盖 | API 和导出均需登录 |
| TR-008 | 等待有反馈，不整页阻塞 | 乐观写入、后台同步、局部 AI 状态、位置恢复 | 已覆盖 | 保存前后无新增导航或骨架屏 |

结论建议统一使用：

- `已覆盖`
- `部分覆盖`
- `无明确证据`
- `与需求冲突`

## 5. 主要发现

### 5.1 阻塞问题

- 当前未发现阻塞合入的问题。

### 5.2 重要风险

- 当前未发现重要风险。

### 5.3 一般建议

- 当前无需在交付前补充的一般建议。

### 5.4 待确认问题

#### CR-901

- 严重级别：待确认问题
- 标题：正式部署与线上回滚尚未执行
- 证据：本轮验证使用本机生产构建和真实外部服务，未对 Vercel 产生外部写入。
- 影响：不影响第二版本地使用；对公网可用性、环境变量和回滚仍缺线上证据。
- 建议：按 `上线checkList.md` 执行发布，部署后重跑生产主链路。

## 6. 验证证据与缺口

| 类别 | 证据 | 当前结论 |
|---|---|---|
| 单元/集成测试 | `npm test`：13 个文件、66 个测试通过 | 通过 |
| 构建/类型检查 | `npm run lint`、`npm run typecheck`、`npm run build` 均通过 | 通过 |
| 组件单测/E2E | 工作区交互测试；真实链路脚本覆盖两账号、AI、关系和导出 | 通过 |
| 视觉回归/截图比对 | 实际手机视口和 Mac 视口已检查 | 主要布局通过，无像素级设计稿 |
| 手工验证（浏览器/真机） | 连续写入、AI、关系、导入导出、滚动恢复、离线刷新和重试 | 浏览器手机视口与 Mac 通过；未验证实体手机安装 |
| 监控/日志/回滚准备 | 本地错误状态和可重试路径已验证 | 正式环境待发布后验证 |

补充说明：

- 真实全链路结果为 `PASS`；DeepSeek 首段观测值为 2472、2316、2124、1874、2245 毫秒。
- 大文件验证包含 4.5MB 以上流式导出与 JSON 可解析性。
- 真实浏览器保存前后 `performance` 导航记录保持为 1，页面未出现工作区骨架屏；AI 结果落下后操作锁定，新增用户内容后恢复。

## 7. 风险视角扫描

| 视角 | 结论 | 备注 |
|---|---|---|
| 需求对齐 | 无发现 | PRD P0 行为均有实现和验收证据 |
| 正确性与边界 | 无发现 | 覆盖空数据、并发、离线、重试、大文件和 AI 失败边界 |
| 数据/迁移 | 无发现 | 迁移可重复，旧表保留且零新写入 |
| 配置/兼容性 | 无发现 | 秘钥仅服务端；手机/Mac 布局及 PWA 边界已验证 |
| 安全/权限 | 无发现 | Cookie 会话、服务端归属过滤、RLS 和基础安全响应头 |

**安全/权限子维度明细**：

| 子维度 | 结论 | 关键证据或备注 |
|---|---|---|
| 6A 认证与会话 | 无发现 | Supabase Cookie 会话，公开注册关闭 |
| 6B 授权与数据越权 | 无发现 | API 要求登录，所有仓储按 userId 过滤，两账号实测隔离 |
| 6C 输入校验与注入 | 无发现 | 服务端大小与字段校验；React 默认转义，无 HTML 直接注入 |
| 6D 敏感数据保护 | 无发现 | 密钥仅服务端环境/钥匙串，无正文日志 |
| 6E 业务逻辑安全 | 无发现 | 客户端请求幂等，关系唯一约束与一次状态决定 |
| 6F 安全配置与依赖 | 无发现 | `npm audit --omit=dev --audit-level=high`：0 漏洞；已配置 nosniff、DENY、Referrer 和 Permissions Policy |
| 可观测性/回滚 | 待发布后验证 | 本地失败状态完整；线上监控本轮未执行 |

## 8. 待补充确认

- Vercel 部署地址、环境变量注入、正式环境验收和回滚演练尚未执行。

## 9. 建议后续动作

1. 保留当前未提交变更，待用户决定提交与发布时机。
2. 正式发布时执行 `上线checkList.md`，并在线上重跑全链路验证。
