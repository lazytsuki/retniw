# 【PRD】retniw

## 文档修订记录

| 版本 | 内容 | 作者 | 日期 |
| --- | --- | --- | --- |
| V2.0 | 调整核心流程与首版范围 | 项目所有者 | 2026/8/19 |

## 概述

### 需求背景、目标及收益

念头刚出现时，通常不值得先命名、分类或整理；但只保存一句话，又常常承接不住接下来的思考。

retniw 让一句话先留下，也允许它自然继续。用户可以接着写、引入外部内容、按需调用 AI，或者直接离开。内容、完整过程和关系都可以导出，不要求用户把全部上下文留在 retniw。

### 用户分析

| 用户 | 使用场景 | 核心关注点 |
| --- | --- | --- |
| 个人用户，首位用户为项目所有者 | 随时捕捉念头，在手机或 Mac 上继续、回看、导入和导出 | 打开即写、随时继续、内容可迁移、等待有反馈 |

### 首版结果判断

| 目标 | 判断方式 |
| --- | --- |
| 捕捉没有负担 | 打开后可以直接输入并离开，不要求标题、分类或后续操作 |
| 短内容可以自然变长 | 保存后仍停留在当前内容中，可以继续追加，不必新建条目 |
| 基本使用不依赖 AI | 记录、继续、导入、导出和回看均可独立完成 |
| 内容可以流动 | 能导入常用文本文件，也能导出完整内容和关系数据 |
| 等待过程可理解 | 保存、同步、AI 处理和关系检查各自显示状态，不出现无反馈的跳转或刷新 |

## 总体流程

```text
输入或导入内容
  ↓
内容立即出现在当前页面并保存在本地
  ↓
后台同步，不打断继续输入
  ↓
用户继续写、按需调用 AI、离开或导出
  ↓
系统在后台寻找可能的关系
  ↓
有候选时提示，用户决定是否保留
```

任何一步失败，都不撤销已经写下的内容，也不阻止用户继续写或导出。

## 功能需求

### 功能列表

| 功能模块 | 子功能点 | 调整类型 | 调整内容 | 优先级 |
| --- | --- | --- | --- | --- |
| 输入 | 直接记录 | 重构 | 一句话与连续追加使用同一入口，不预先选择内容形态 | P0 |
| 输入 | 引入外部内容 | 新增 | 支持粘贴、Markdown 和纯文本文件 | P0 |
| 继续 | 原处追加 | 新增 | 保存后不跳走，可以在当前内容后继续写 | P0 |
| AI | 按需推进 | 重构 | 用户需要时才让 AI 提问、回应、整理或寻找联系 | P0 |
| 关系 | 候选关系 | 重构 | 后台检查关系，有结果时提示，用户决定是否保留 | P0 |
| 输出 | 复制与导出 | 新增 | 支持复制单段、导出完整过程和导出关系数据 | P0 |
| 回看 | 最近记录与恢复位置 | 重构 | 跨端打开同一段思考并恢复上次位置 | P0 |
| 语音与更多格式 | 扩展输入 | 后续 | 语音、图片、PDF 和第三方平台接入后续再做 | P2 |

### 输入与保存

#### 直接记录

- 打开 retniw 后直接出现输入区域。
- 输入前不要求标题、分类、标签或所属项目。
- 用户按 Enter 保存当前内容，按 Shift+Enter 换行；中文输入法选词期间不提交。
- 内容保存后留在原页面，并立即提供继续输入的位置。
- 用户可以只留下一句话后离开，不需要处理 AI 提示或关系候选。

#### 保存状态

- 用户输入的内容先在当前页面出现并保存在本地，再进行后台同步。
- 页面分别显示本地已保存、正在同步和同步失败。
- 同步失败时保留内容并允许重试，不把失败内容显示成已经同步。
- 刷新、关闭或跨端打开后，已经同步的内容按原顺序恢复。

#### 验收标准

- 保存后不发生整页跳转或空白刷新。
- 用户可以连续追加至少三段内容，中间不需要返回首页或新建条目。
- 断网时内容仍留在当前页面；恢复网络后可以继续同步。
- 不调用 AI，也能完成记录、继续和回看。

### 连续思考

- 用户后续写下的内容按时间接在当前过程之后。
- 用户可以随时离开，回来后继续追加。
- 每段用户原文独立保留，后续内容和 AI 输出不能覆盖它。
- 页面按内容发生顺序和来源展示用户原文、导入内容与 AI 输出。

#### 验收标准

- 一句话可以独立保存，也可以在同一位置继续发展。
- AI 输出结束后，输入区域仍然可用，不出现流程终点。
- 用户连续追加内容时，系统不会自动为每段内容生成 AI 回复。
- 回看时能分清用户原文、外部内容和 AI 输出。

### AI 按需介入

- AI 默认不回应每次输入。
- 用户可以在当前内容中主动选择推进、追问、整理或寻找联系。
- AI 只使用当前过程和用户明确引入的上下文。
- AI 开始处理后立即显示状态，并逐步展示已经返回的内容。
- AI 失败时保留当前内容，用户可以重试，也可以不使用 AI 继续写。
- AI 输出与用户原文分别保存，不自动改写原文。

#### 验收标准

- 用户未调用 AI 时，不产生自动回复。
- 调用后 100 毫秒内出现处理状态，不出现无反馈等待。
- 网络正常时，以 80% 请求在 3 秒内出现首段可读内容作为首版目标。
- AI 返回过程中用户仍能离开；已经写下的内容不受影响。

### 引入外部内容

- 用户可以直接粘贴外部文字，也可以导入 `.md` 和 `.txt` 文件。
- 导入内容进入当前过程或开始一个新过程，由用户在导入时决定。
- 文件导入保留文件名；粘贴导入可以补充来源名称，也可以留空。
- 导入内容保持原样，AI 不自动改写或拆分。
- 单次导入失败不影响当前已有内容。

#### 验收标准

- Markdown 和纯文本文件能够完整导入中文、换行和常见标点。
- 导入后能区分外部内容和用户在 retniw 中写下的内容。
- 不调用 AI 也能查看和导出导入内容。
- 不支持的文件给出明确提示，不创建空内容。

### 关系

- 关系检查在后台独立进行，不阻塞记录、继续写或导出。
- 系统一次最多提示一个值得确认的候选关系。
- 候选展示两端原文和一句简短理由。
- 用户可以保留、否定或暂不处理。
- 未经用户确认，候选不能成为长期关系；否定后不重复提出同一关系。

#### 验收标准

- 关系检查期间当前输入始终可用。
- 只有一段内容时不虚构关系。
- 确认和否定均不改变两端原文。
- 关系检查失败时不阻止用户继续或导出。

### 复制与导出

- 用户可以复制单段内容，不附带界面文案。
- 用户可以把一个完整过程导出为 Markdown，保留内容顺序、时间、来源和作者类型。
- 用户可以导出全部内容和已确认关系的结构化数据。
- 结构化数据包含稳定标识、原文、时间、来源、作者类型和关系两端标识。
- 导出不依赖 AI，也不要求先整理或确认关系。

#### 验收标准

- Markdown 离开 retniw 后仍能按顺序阅读完整过程。
- 结构化数据离开 retniw 后仍能识别每段内容及已确认关系。
- 导出失败不改变或删除原内容。
- 用户能够导出自己的全部数据，不存在只能在页面内读取的必要字段。

### 回看与跨端

- 首页按每段思考展示最近记录，同一段思考中的追加内容保持在一起。
- 打开某段内容后恢复上次阅读和输入位置。
- 手机和 Mac 使用同一份内容、顺序、来源和关系。
- 同步未完成或失败时，两端显示真实状态，不用旧内容覆盖新输入。

## 非功能需求

### 内容安全

- 个人内容默认仅本人可访问，不提供公开链接或团队协作。
- 日志和错误信息不记录正文、导入内容、身份凭据或密钥。
- AI 只接收完成当前操作所需的上下文；导出不经过 AI。

### 内容可靠性

- 用户原文、导入内容和 AI 输出分别保存，后续处理不能覆盖原文。
- AI、同步或关系检查失败时，已经输入的内容仍能恢复和导出。
- 导出格式需要有版本号，后续升级不能让旧文件失去解释方式。

### 响应与状态

- 输入内容在下一帧内出现在页面，不等待网络。
- 保存、同步、AI 处理和关系检查使用独立状态，不能用一个 loading 覆盖全部过程。
- 页面切换需要先出现当前内容或加载骨架，不出现整页空白。
- AI 使用流式返回；关系检查不占用 AI 主交互的等待状态。

### 终端适配

- 手机优先保证打开即写、继续输入和单手操作。
- Mac 利用横向空间展示当前内容、来源和关系，但不形成另一套功能。
- 两端使用相同的内容顺序、来源标记和操作，只调整页面排布。

## 非目标

- 首版不接入 Notion、Obsidian、微信等第三方平台 API。
- 不解析图片、音频、PDF 或复杂富文本。
- 不做团队协作、公开发布或社交互动。
- 不要求用户维护文件夹、标签或可视化图谱。
- 不自动把内容改写成文章、任务、项目计划或最终观点。
- 首版不引入向量库或独立图数据库。

## 模板来源

- 模板：`【PRD】基础产品需求文档`
- 来源：`https://joyspace.jd.com/pages/72xNrWDr3E1KMOymaOLx`
- 导出时间：`2026-06-30T13:50:00.000Z`
