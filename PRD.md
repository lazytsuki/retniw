# 【PRD】retniw

## 文档修订记录

| 版本 | 内容 | 日期 |
| --- | --- | --- |
| V2.4 | 收口记录、联系与回看，重做归档和删除 | 2026/8/24 |
| V2.3 | 增加想法停靠、单层合集、历史内容管理与账号创建 | 2026/8/22 |
| V2.2 | 收口页面文案、侧栏滚动与点击响应 | 2026/8/21 |
| V2.1 | 明确工作区导航和首次使用 | 2026/8/21 |
| V2.0 | 调整核心流程与功能范围 | 2026/8/19 |

## 概述

### 需求背景、目标及收益

念头刚出现时，通常不值得先命名、分类或整理；但只保存一句话，又常常承接不住接下来的思考。

retniw 让一句话先留下，也允许它自然继续。用户可以接着写、引入外部内容、按需调用 AI，或者直接离开。内容、完整过程和关系都可以导出，不要求用户把全部上下文留在 retniw。产品只需要一条链路：先记下来，慢慢表达，之后回来时看见原本分散的想法之间可能有什么联系。

工作区需要清楚呈现新建、切换、停靠和历史管理入口，让用户不依赖教程就能理解内容去向。账号支持自行创建，内容默认私有。当前想法中的 AI 只读取当前内容；跨想法能力由独立回看承接，既能主动扫描已经积累的想法，也能在开启后处理新保存的内容。

### 用户分析

| 用户 | 使用场景 | 核心关注点 |
| --- | --- | --- |
| 个人用户 | 随时捕捉念头，在手机或 Mac 上继续、回看、导入和导出 | 不看说明也能开始、随时另起或切换想法、内容可迁移、等待有反馈 |

### 产品目标

| 目标 | 判断方式 |
| --- | --- |
| 捕捉没有负担 | 打开后可以直接输入并离开，不要求标题、分类或后续操作 |
| 短内容可以自然变长 | 保存后仍停留在当前内容中，可以继续追加，不必新建条目 |
| 基本使用不依赖 AI | 记录、继续、导入、导出和浏览以前的想法均可独立完成 |
| 内容可以流动 | 能导入常用文本文件，也能导出完整内容和关系数据 |
| 等待过程可理解 | 保存、同步、当前想法 AI 和跨想法候选各自运行，不出现无反馈的跳转或刷新 |
| 产品不用说明书解释 | 首次用户能找到新想法和已有想法，并将产品理解为承接想法而非 AI 聊天 |
| 长想法可以自然停下 | 用户能留下停靠点并回到全部想法，不被要求形成最终结论或自动归档 |
| 多条想法可以一起回看 | 用户明确开启回看后，可以主动串联已积累的想法；保存新内容仍不受阻塞，有原文依据的联系候选在回看中出现，并由用户保留或忽略 |
| 历史内容可管理 | 用户能把想法移入单层合集、归档或删除；归档作为历史子视图，删除经强确认后不可恢复 |
| 用户可独立进入 | 从登录页用邮箱和自设密码创建账号并直接开始 |

## 总体流程

```text
创建账号或登录 → 先记下来 → 慢慢表达 → 回来看见联系
  ↓
输入或导入内容，原文立即出现在当前页面并保存在本地
  ↓
后台同步，不打断继续输入
  ↓
用户继续写、另起想法、切换已有想法、按需调用当前想法中的 AI、离开或导出
  ↓
需要停下时选择“先到这里”，可选留一句并回到全部想法
  ↓
之后按需移入合集、归档或删除；归档进入历史子视图，删除经确认后不可恢复
  ↓
用户首次明确开启“回看”后，可主动串联已有想法；之后保存新内容时也会后台比较必要的新旧用户原文
  ↓
有原文依据的候选进入回看，用户决定保留或忽略
```

未开启回看时，保存不会触发跨想法模型处理。开启后，后台比较失败也不撤销已经写下的内容，不阻止用户继续写、离开或导出。

## 功能需求

### 功能列表

| 功能模块 | 子功能点 | 调整类型 | 调整内容 | 优先级 |
| --- | --- | --- | --- | --- |
| 输入 | 直接记录 | 重构 | 一句话与连续追加使用同一入口，不预先选择内容形态 | P0 |
| 输入 | 引入外部内容 | 新增 | 支持粘贴、Markdown 和纯文本文件 | P0 |
| 继续 | 原处追加 | 新增 | 保存后不跳走，可以在当前内容后继续写 | P0 |
| AI | 当前想法按需继续 | 重构 | 正文旁只保留“帮我接着想”；整理进入更多操作，均由用户主动调用 | P0 |
| 联系 | 跨想法候选 | 重构 | 回看开启后，用户可主动扫描已有想法；保存新内容时也会异步比较必要的新旧用户原文，只生成有依据的候选 | P0 |
| 回看 | 候选与保留的联系 | 新增 | 独立展示候选两端原文，用户保留或忽略；保留后的联系仍可回来查看 | P0 |
| 回看 | 开启与关闭 | 新增 | 默认关闭，首次明确说明处理范围并由用户开启，之后可随时关闭 | P0 |
| 输出 | 复制与导出 | 新增 | 支持复制单段、导出完整过程和导出关系数据 | P0 |
| 历史 | 以前的想法与恢复位置 | 重构 | 随时打开任一已有想法并恢复上次位置 | P0 |
| 导航 | 新建、切换与继续 | 重构 | 固定提供“写新想法”和“以前的想法”，当前想法直接留在主区 | P0 |
| 停靠 | 先到这里 | 新增 | 留下可选停靠点并回到全部想法，不自动归档 | P0 |
| 合集 | 单层合集 | 新增 | 一个合集可包含多个想法，一个想法最多属于一个合集 | P0 |
| 管理 | 移入、归档、删除 | 重构 | 移入改变合集归属；归档移出常用列表；删除经强确认后永久执行 | P0 |
| 历史 | 归档子视图 | 重构 | 从“以前的想法”进入独立归档列表，不再与最近内容平铺 | P0 |
| 浮层 | 单开与关闭 | 重构 | 打开新浮层时关闭旧浮层，点击空白或按 Escape 收起 | P0 |
| 响应 | 详情切换 | 重构 | 保留工作区外壳，只加载当前内容，摘要查询不扫描完整长文 | P0 |
| 首次使用 | 用结构说明定位 | 重构 | 空白状态先说明写下与保存，AI 在有内容且用户需要时才出现 | P0 |
| 账号 | 创建账号 | 新增 | 登录页支持邮箱和密码创建独立账号；内容仍仅本人可见 | P0 |
| 账号 | 身份回显 | 新增 | 右上角回显邮箱，支持设置昵称并作为受限称呼标签 | P0 |
| 语音与更多格式 | 扩展输入 | 后续 | 语音、图片、PDF 和第三方平台接入后续再做 | P2 |

### 工作区导航与首次使用

- 全局只使用“写新想法”和“以前的想法”两个导航动作；当前想法是主区内容状态，不再做成第三个同级按钮。
- “写新想法”进入空白输入状态；“以前的想法”用第一段原文摘录展示已有内容；当前主区保留完整过程并允许继续输入。
- 返回只表示回到浏览历史，不承担新建功能；退出收进账号入口，不占据主要导航位置。
- 桌面端常驻展示写新想法入口和已有想法列表；移动端固定展示“以前的想法”，进入已有内容或回看时同时展示“写新想法”，空白首页不重复展示正在执行的新建动作。
- 桌面端侧栏可展开或收起；收起后使用固定窄轨展示品牌、写新想法、归档和回看图标。归档和回看固定在底部，历史列表只滚动中间区域。移动端使用抽屉，不显示桌面端窄轨。
- 空白状态只说“写下你正在想的。”，输入提示为“从这里开始写”；已有内容后，输入区域使用可见的“继续写”标签和“补充一个新的点，或继续刚才的思路”提示。输入区域在桌面和移动端都必须通过边界、底色和聚焦状态被识别为可编辑表面，不依赖占位文字单独承担入口提示。不使用教程弹窗、示例聊天、功能说明页或替产品抒情的宣传句。
- 没有内容时不展示 AI 操作；有内容后，正文旁只出现一个“帮我接着想”入口，整理收进更多操作。跨想法候选统一进入“回看”，不与当前输入动作平铺。
- “写新想法”在已有内容和回看中持续可见，不依赖一次性教程提示；空白首页本身就是新想法状态，不重复展示。进入首页时必须卸载旧工作区状态，不能只改变 URL。
- 历史列表只纵向滚动，选中态和悬停态不得撑出横向滚动；历史详情按用户实际选择加载，不在首页并发预取整页详情。
- “以前的想法”先展示最近内容，再提供合集、归档和回看入口；三者是次级内容入口，不与“写新想法”或每条最近内容平铺。
- “归档”打开独立历史子视图，子视图顶部显示返回和“归档”，不使用“全部 / 归档 / 已删除”横向标签。

#### 验收标准

- 未听过产品介绍的用户能在 5 秒内找到新想法和已有想法入口。
- 至少 5 名首次用户独立体验 90 秒后，至少 4 人将产品描述为记录、承接或继续想法，而不是 AI 聊天。
- 首次用户能在不调用 AI 的情况下完成记录、打开以前的想法和另起想法。
- 手机与 Mac 上的入口含义一致，不依靠手势、返回箭头或品牌标识完成切换。
- 点击新建或历史项后 100 毫秒内出现就地状态；同一次详情切换只发起一次页面数据请求。

### 账号入口

- 登录页并列提供“登录”和“创建账号”。
- 新用户填写邮箱、自设密码并再次确认密码；密码至少6个字符。
- 注册成功后先显示“账号创建成功”和进入状态，然后自动请求工作区；同时提供“立即进入”备用入口。
- 工作区首次读取遇到短暂错误时，服务端只重试一次相同的有界查询；重试仍失败时显示真实错误，不伪装为空列表。
- 公开注册只开放账号入口，不公开任何想法、合集、关系或导出内容；所有用户数据继续按账号隔离。
- 登录后右上角显示当前邮箱；可以设置、更新或清除最多24个字符的昵称。有昵称时顶栏优先显示昵称，账号面板始终显示完整邮箱。
- 昵称只作为当前想法 AI 和澄清问题中的称呼标签，不参与关系匹配，不被当作身份、偏好、事实或指令。
- 注册失败使用通用提示，不向未登录用户暴露某个邮箱是否已经存在。
- 邮箱确认和密码找回依赖可靠的邮件投递能力，启用前需要先配置正式 SMTP。

#### 验收标准

- 用户可以独立创建账号并开始记录。
- 注册、登录与退出均使用独立账号；账号A不能读取账号B的想法、合集、停靠点和关系。
- 创建账号提交期间有明确状态；重复邮箱、无效邮箱、短密码和两次密码不一致不会进入产品。
- 创建成功后不需要手动刷新；用户能看到当前邮箱，昵称修改后顶栏与后续称呼使用同一个值。

### 想法停靠与管理

#### 先到这里

- “先到这里”只结束这一次思考，不判断想法已经完成。
- 用户可以留一句当前判断或下次入口，也可以不写，随后返回全部想法。
- 该动作不自动归档，不调用 AI，也不改变合集归属。
- 下次打开时恢复到停靠点和继续输入位置；后续新增内容不会覆盖旧停靠点。

#### 单层合集

- 合集只有一层，不支持子合集、文件夹树或标签体系。
- 一个合集可以包含多个想法；一个想法最多移入一个合集，也可以不属于合集。
- 记录时不要求选择合集；“移入”时可以选择已有合集或就地新建。
- 删除合集只解除归属，不删除其中的想法。
- 跨主题关系继续由“联系”承担，不用多个合集重复表达。

#### 历史条目操作

- 所有端均保留可见的更多入口，菜单使用“移入、归档、删除”三个简短动作。
- 桌面端支持悬停或键盘聚焦显示更多入口，右键打开同一组操作。
- 移动端支持左滑露出“归档”和“删除”，长按打开包含“移入、归档、删除”的操作抽屉。
- 已归档条目使用“取消归档、移入、删除”；取消归档后回到最近内容，合集归属不变。
- “删除”无论从更多入口、右键、长按还是左滑进入，都先打开强确认，不由手势直接执行。
- 确认文案只说明事实：“删除这个想法？”“删除后无法恢复，相关联系也会一并删除。”；按钮使用“取消”和“删除”。确认后永久删除想法及其内容，不提供恢复或“已删除”区域。
- 已经软删除的数据继续隐藏，不展示、不恢复，也不自动清理。
- 删除是危险操作，在视觉和分组上与移入、归档区分。
- 三个动作可以使用线性 SVG 辅助识别，但文字始终保留。

#### 归档子视图

- 最近内容与归档内容不使用同级标签切换；“归档”从“以前的想法”进入独立子视图。
- 子视图只展示已归档想法及必要时间信息，不重复展示合集管理或删除区域。
- 归档为空时只显示“还没有归档的想法。”，并保留回到以前想法的入口。
- 归档只改变常用列表中的可见性，不改变原文、合集归属、停靠点或已确认联系。

#### 浮层规则

- 同一时刻只保留一个菜单、抽屉或非模态操作层。
- 打开另一个浮层、点击空白、按 Escape 或完成选择后，当前浮层收起。
- 模态导入和删除确认出现时，其他菜单必须先关闭；关闭后焦点返回原触发位置。

#### 验收标准

- 长想法选择“先到这里”后立即返回全部想法，下次打开恢复到停靠点，且该想法未被自动归档。
- 一个想法不能同时属于两个合集；删除合集后其中的想法仍可从全部想法中打开。
- 移动端左滑、长按与桌面端更多入口、右键执行相同的归档和删除语义。
- 进入归档后不会看到“全部”或“已删除”标签；取消归档后条目回到最近内容，合集归属仍在。
- 删除确认明确说明无法恢复；确认后想法、内容和相关联系均不可再读取，界面没有恢复入口。
- 已经软删除的数据不出现在最近内容、归档、合集、回看或导出中。
- 打开账号菜单后再打开想法菜单，前一个自动关闭；点击空白或按 Escape 后当前菜单收起。

### 输入与保存

#### 直接记录

- 打开 retniw 后直接出现输入区域。
- 输入前不要求标题、分类、标签或所属项目。
- 用户按 Enter 保存当前内容，按 Shift+Enter 换行；中文输入法选词期间不提交。
- 内容保存后留在原页面，并立即提供继续输入的位置。
- 用户可以只留下一句话后离开，不需要处理 AI 提示或关系候选。

#### 保存状态

- 用户输入的内容先在当前页面出现并保存在本地，再进行后台同步。
- 页面分别显示本地已保存、正在同步和同步失败。
- 同步失败时保留内容并允许重试，不把失败内容显示成已经同步。
- 刷新、关闭或跨端打开后，已经同步的内容按原顺序恢复。
- 回看关闭时，保存和同步不触发跨想法模型处理。
- 回看开启时，新内容同步成功后在后台准备联系候选；该处理不占用保存状态，也不阻止继续输入、切换或离开。

#### 验收标准

- 保存后不发生整页跳转或空白刷新。
- 用户可以连续追加至少三段内容，中间不需要返回首页或新建条目。
- 断网时内容仍留在当前页面；恢复网络后可以继续同步。
- 不调用 AI，也能完成记录、继续和浏览以前的想法。
- 回看开启后，即使跨想法比较失败，原文仍显示为已同步，输入和切换保持可用。

### 连续思考

- 用户后续写下的内容按时间接在当前想法之后。
- 用户可以随时离开，回来后继续追加。
- 每段用户原文独立保留，后续内容和 AI 输出不能覆盖它。
- 页面按内容发生顺序和来源展示用户原文、导入内容与 AI 输出。

#### 验收标准

- 一句话可以独立保存，也可以在同一位置继续发展。
- AI 输出结束后，输入区域仍然可用，不出现流程终点。
- AI 输出不会自动触发“先到这里”；停靠始终由用户决定。
- 用户连续追加内容时，系统不会自动为每段内容生成 AI 回复。
- 回看时能分清用户原文、外部内容和 AI 输出。

### 当前想法中的 AI

- AI 默认不回应每次输入。
- 用户可主动选择“帮我接着想”，由 AI 在问一个具体问题和指出一个可继续角度之间选择一个；用户不需要预判“推进”和“追问”的差别。
- 整理只在当前想法的更多操作中出现，不与继续写或“帮我接着想”平铺。
- “帮我接着想”和整理只使用当前想法中用户写下或明确引入的内容，不自动带入其他想法。
- AI 开始处理后立即显示状态，并逐步展示已经返回的内容。
- AI 失败时保留当前内容，用户可以重试，也可以不使用 AI 继续写。
- AI 输出与用户原文分别保存，不自动改写原文。

#### 验收标准

- 用户未调用 AI 时，不产生自动回复。
- 调用后 100 毫秒内出现处理状态，不出现无反馈等待。
- 网络正常时，至少 80% 的请求在 3 秒内出现首段可读内容。
- AI 返回过程中用户仍能离开；已经写下的内容不受影响。

### 引入外部内容

- 用户可以直接粘贴外部文字，也可以导入 `.md` 和 `.txt` 文件。
- 导入内容进入当前想法或开始一个新想法，由用户在导入时决定。
- 文件导入保留文件名；粘贴导入可以补充来源名称，也可以留空。
- 导入内容保持原样，AI 不自动改写或拆分。
- 单次导入失败不影响当前已有内容。

#### 验收标准

- Markdown 和纯文本文件能够完整导入中文、换行和常见标点。
- 导入后能区分外部内容和用户在 retniw 中写下的内容。
- 不调用 AI 也能查看和导出导入内容。
- 不支持的文件给出明确提示，不创建空内容。

### 记录、联系与回看

三层各自承担一种职责，不把 AI 结果混入用户原文。

- 记录：保存用户写下或导入的原文。原文是主体，不因联系候选、整理或删除其他想法而被改写。
- 联系：DeepSeek 比较必要的新旧用户原文后提出候选。候选必须指向两端原文及一条简短依据，不是一段泛化总结。
- 回看：独立承接待判断候选和用户已经保留的联系。它不提供聊天输入，也不代替当前想法的继续写。

#### 开启与关闭

- 跨想法主动回看默认关闭；新账号和已有账号都不预先开启。
- 用户首次进入回看时，页面说明主动串联和后续保存各自何时处理内容。用户点击“开启并开始串联”后，才把最多20条最近想法的开头和最新一段原文交给 DeepSeek；它最多提出3条可能的联系，不会改写或自动保留。开启后，后续保存新内容也会继续寻找联系。
- 用户可以随时关闭。关闭后不再处理后续保存；已有候选和已经保留的联系仍可查看、保留或忽略。
- 当前想法中的“帮我接着想”和整理不受回看开关影响，仍只在用户主动调用时运行。

#### 候选产生与判断

- 回看开启后，用户可以主动点击“开始串联”。系统比较最多20条最近想法的开头和最新一段原文；已经待判断或已保留的想法对不再参与，已忽略的想法对只有在任一端新增用户原文或导入内容后才重新判断。单次最多产生三条候选；本次失败可由用户重试，不占用保存后的entry认领状态。
- 回看开启后，每次用户原文或导入内容同步成功，系统在响应完成后异步比较当前内容与必要的历史用户原文；历史 AI 输出不参与比较。
- 必要历史可以来自最近内容或归档内容；已删除和旧软删除内容不参与，合集归属不构成内容隔离。
- 每次最多生成三条候选。没有充分原文依据时不生成，不用数量填满页面。
- 每条候选按时间并列展示“后来写的”和“更早写的”两段原文，以及一条说明联系所在的短句；既适用于保存后产生的候选，也适用于主动扫描已有想法。
- 用户只做“保留”或“忽略”。保留后成为长期联系；忽略会收起当前理由。两端内容没有变化时保持收起，任一端新增用户原文或导入内容后，系统可以依据新内容提出新的理由，再交给用户判断。
- 未经用户保留，候选不能写入已确认联系，不能续写用户内容、替用户确认观点，也不能自动移入合集、归档或分类。
- 异步比较失败不改变保存结果，不弹出阻断输入的错误；之后有新内容时仍可继续产生候选。

#### 验收标准

- 回看默认关闭；未开启时连续保存三段内容，不发生跨想法 DeepSeek 请求。
- 首次开启前能看到处理对象、触发时机和候选边界；用户未点击“开启并开始串联”时状态不变，也不读取历史内容。
- 开启并开始串联后只处理当前账号最多20条最近想法的用户原文或导入片段，最多新增三条候选；供应商失败与“没有新联系”使用不同反馈，失败后可以重试。
- 开启后保存内容立即完成；候选晚到、超时或失败都不延长保存响应，不改变原文同步状态。
- 候选只引用当前账号的用户原文或导入内容，排除 AI 输出和其他账号内容。
- 每条候选都能回到两端原文；只有一条或没有足够历史内容时不虚构联系。
- 保留和忽略均不改变两端原文；忽略后两端内容不变时不再出现，任一端新增用户原文或导入内容后可以基于新理由再次出现；保留后可在回看中重新打开。
- 关闭回看后新增内容不再触发跨想法比较，关闭前已有候选和联系仍可处理与查看。

### 复制与导出

- 用户可以复制单段内容，不附带界面文案。
- 用户可以把一个完整过程导出为 Markdown，保留内容顺序、时间、来源和作者类型。
- 用户可以导出全部内容和已确认关系的结构化数据。
- 结构化数据包含稳定标识、原文、时间、来源、作者类型和关系两端标识。
- 导出不依赖 AI，也不要求先整理或确认关系。

#### 验收标准

- Markdown 离开 retniw 后仍能按顺序阅读完整过程。
- 结构化数据离开 retniw 后仍能识别每段内容及已确认关系。
- 导出失败不改变或删除原内容。
- 用户能够导出自己的全部数据，不存在只能在页面内读取的必要字段。

### 以前的想法、回看与跨端

- “以前的想法”按每段思考展示已有记录，同一段思考中的追加内容保持在一起。
- 打开某段内容后恢复上次阅读和输入位置。
- “回看”在桌面侧栏与移动端以前想法面板中使用同一名称和层级；待判断候选数量可以提示，但不弹窗打断记录。
- 候选、已保留联系、忽略状态和回看开关随账号同步；换设备后继续使用同一套判断状态和重新判断规则。
- 手机和 Mac 使用同一份内容、顺序、来源和关系。
- 桌面端常驻已有想法列表；移动端从固定入口展开列表，不把切换入口放在长内容末尾。
- 同步未完成或失败时，两端显示真实状态，不用旧内容覆盖新输入。

## 非功能需求

### 内容安全

- 个人内容默认仅本人可访问，不提供公开链接或团队协作。
- 公开注册不等于公开内容；任何列表、详情、管理和导出接口仍需有效会话并按用户ID过滤。
- 日志和错误信息不记录正文、导入内容、身份凭据或密钥。
- 用户主动调用“帮我接着想”或整理时，DeepSeek 只接收当前想法中必要的用户原文与导入内容。
- 用户开启回看后，DeepSeek 才会在用户主动串联或保存新内容后接收必要的用户原文与导入内容；不发送历史 AI 输出、已删除内容或其他账号内容。
- 首次开启前必须说明上述处理范围；用户关闭后，后续保存不再触发跨想法处理。导出始终不经过 AI。
- 使用行为只记录当天进入工作区、打开回看、主动串联结果和从联系打开原文；事件字段不包含正文、AI 输入输出或账号资料。

### 内容可靠性

- 用户原文、导入内容和 AI 输出分别保存，后续处理不能覆盖原文。
- AI、同步或跨想法候选处理失败时，已经输入的内容仍能恢复和导出。
- 导出格式需要有版本号，后续升级不能让旧文件失去解释方式。

### 响应与状态

- 输入内容在下一帧内出现在页面，不等待网络。
- 保存、同步、当前想法 AI 和跨想法候选使用独立状态，不能用一个 loading 覆盖全部过程。
- 页面切换需要先出现当前内容或加载骨架，不出现整页空白。
- 详情切换时保留页头、导航和已加载历史，只在当前内容区显示就地状态。
- 历史摘要读取量不能随每个想法的正文长度线性增长；列表只读取展示摘要所需的内容。
- 当前想法 AI 使用流式返回；跨想法候选在保存响应后异步处理，不占用保存或当前想法 AI 的等待状态。
- 使用行为记录失败不改变保存、主动串联或原文跳转结果。

### 终端适配

- 手机优先保证打开即写、继续输入和单手操作。
- Mac 利用横向空间展示当前内容、来源和关系，但不形成另一套功能。
- 两端使用相同的内容顺序、来源标记和操作，只调整页面排布。

## 非目标

- 不接入 Notion、Obsidian、微信等第三方平台 API。
- 不解析图片、音频、PDF 或复杂富文本。
- 不做团队协作、公开发布或社交互动。
- 不做多层文件夹、子项目、标签体系或可视化图谱；合集只保留一层。
- 不自动把内容改写成文章、任务、项目计划或最终观点。
- 不把跨想法回看做成聊天机器人、自动复盘长文或定时推送；只承接用户主动扫描或保存后产生的候选联系。
- 不替用户保留、忽略或解释候选，不依据候选自动分类、移入合集或归档。
- 不引入向量库或独立图数据库。
- 不在记录前要求用户选择合集、标题或分类，不自动给想法分类。
- 不用新手教程、示例聊天或自动 AI 回复解释产品定位。
- 不把“先到这里”解释成完成任务、最终观点或 AI 总结。
- 不提供回收站、“已删除”列表或删除恢复；已经软删除的数据保持隐藏，不自动永久清理。
