# CODE-REVIEW

## 1. 文档概览

- 文档状态：Final
- 输出文件：`CODE-REVIEW-M007.md`
- 评审目标：对照PRD V2.1、M-007任务卡和当前工作区变更，评估“零说明书体验收口”的需求对齐、状态边界与验证充分性
- 总体 verdict：`通过`
- 合入建议：`可以进入生产发布`

## 2. 输入来源

### 2.1 需求侧证据

- `PRD.md`：工作区导航、首次使用、主动AI、关系和跨端验收口径。
- `TECH-DESIGN.md`：统一工作区、状态恢复、AI输出边界和关系过滤方案。
- `TASK-M-007-零说明书体验收口.md`：实现范围、明确不变约束与完成定义。
- 用户提供的八张现网截图：指令式AI前缀、四项操作平铺、无语义返回入口、大横条顶栏、旧想法区域层级、侧栏选中边框被裁切、横向滚动和产品文案问题。

### 2.2 变更侧证据

- 对比基线：当前`HEAD`与未提交工作区。
- 变更范围：页面信息架构、桌面与移动导航、AI入口分层、历史列表分页、关系候选内容边界、AI提示词、文案、测试及项目文档。
- 新增主要组件：`ThoughtNavigation`、`ThinkingAssist`、`ThoughtMenu`与`aiOutputForDisplay`；删除旧`AiActions`。
- 变更检查：`git diff --check`通过，未发现空白符或冲突标记问题。

### 2.3 本次评审范围

- 包含：登录后的新建、继续、回看、按需AI、关系检查、导入导出入口、后台同步与导航并发、320至1440像素响应式表现。
- 不包含：数据库结构、认证方案、模型供应商、语音和搜索等新能力；这些内容本轮未改动。

## 3. 评审结论

### 3.1 Verdict 摘要

- 阻塞问题：0
- 重要风险：0
- 一般建议：0
- 待确认问题：0

### 3.2 合入建议

- 当前实现覆盖M-007代码侧完成定义，可以提交并部署；生产发布后需在`https://retniw.cn`重放同一主链路。

### 3.3 非评审范围

- 不以本轮信息架构调整扩展标题、标签、文件夹、搜索、聊天结构、第二模型或数据库实体。
- 首次用户能否在90秒内自然说出产品用途，需要真实用户测试，不由代码审查代替。

## 4. 需求-变更追溯矩阵

| ID | 需求/约束 | 变更证据 | 结论 | 备注 |
|---|---|---|---|---|
| TR-001 | 新建、继续和回看各有自然入口 | `ThoughtNavigation`、`CapturePage`独立key、明确新建状态标记 | 已覆盖 | 当前想法留在正文，不做第三个导航按钮 |
| TR-002 | 不靠说明书表达产品链路 | 空白态“写下你正在想的”及“写在这里/接着写”输入状态 | 已覆盖 | 空白页无AI入口，不使用宣传式长句 |
| TR-003 | AI按需出现且不呈现聊天工具结构 | `ThinkingAssist`、`ThoughtMenu`、`DeepSeekTextProvider` | 已覆盖 | 主区只有“帮我接着想”，整理和找联系分层放置 |
| TR-004 | 历史AI输出不伪装成用户想法 | 关系检查仅传`user/import` entries，旧AI候选自动拒绝 | 已覆盖 | 候选页只展示“现在/以前”两段原文 |
| TR-005 | 旧指令式前缀不再影响阅读 | 新提示词、接口保存归一化和`aiOutputForDisplay` | 已覆盖 | 新旧内容均不显示“可以继续写：” |
| TR-006 | 手机与桌面导航连续且不遮挡输入 | 桌面侧栏、移动常驻双入口、历史面板、响应式样式 | 已覆盖 | 320至1440像素无横向溢出；移动导航与输入无重叠 |
| TR-007 | 新建与后台同步互不污染 | 明确新建和草稿恢复分流；同步回调按`thoughtId`过滤 | 已覆盖 | 未同步旧内容仍由全局outbox继续处理 |
| TR-008 | 内容可轻松进入并完整带走 | 更多菜单保留导入、单想法导出和全量导出 | 已覆盖 | 本轮只调整入口层级，不改变导出格式 |
| TR-009 | 侧栏选中态完整且切换有即时反馈 | 侧栏横向裁切、取消历史项位移、导航处理中状态 | 已覆盖 | 悬停后`clientWidth=scrollWidth=280`，`scrollLeft=0` |
| TR-010 | 不让批量预取拖慢真实点击 | 历史链接关闭批量预取；详情与AI独立查询并行 | 已覆盖 | 首页空闲时详情预取为0；每次详情选择只有1个RSC请求 |

结论建议统一使用：

- `已覆盖`
- `部分覆盖`
- `无明确证据`
- `与需求冲突`

## 5. 主要发现

### 5.1 阻塞问题

- 当前未发现阻塞合入的问题。

### 5.2 重要风险

- 当前未发现需要在发布前处理的重要风险。

### 5.3 一般建议

- 当前没有需要在发布前追加的一般建议。

### 5.4 待确认问题

- 当前没有代码侧待确认问题。

## 6. 验证证据与缺口

| 类别 | 证据 | 当前结论 |
|---|---|---|
| 单元/集成测试 | `npm test -- --run`：13个文件、78个测试通过 | 通过 |
| 构建/类型检查 | `npm run typecheck`、`npm run lint`、`npm run build`均通过 | 通过 |
| 组件单测/E2E | 工作区、AI流、关系过滤、输入语义和路由状态测试；真实账号完整链路回放 | 通过 |
| 视觉回归/截图比对 | `output/playwright/retniw-production-desktop-feedback-final.png`、`retniw-production-mobile-feedback-detail.png`、`retniw-production-mobile-feedback-empty.png` | 页面层级、侧栏边框、文案与移动入口通过；无像素级设计稿 |
| 手工验证（浏览器/真机） | Chromium真实页面验证写下、AI给一步、继续写、整理、另起、旧想法返回和历史面板 | 手机视口与桌面通过；本轮未代替实体手机网络验收 |
| 监控/日志/回滚准备 | 本地与最终生产回放控制台均为0错误、0警告；`npm audit --omit=dev --audit-level=high`为0漏洞 | 通过；生产首轮发现的时区hydration错误已修复并重新发布验证 |

补充说明：

- 浏览器在320、375、768、1024、1440像素逐一读取`scrollWidth/clientWidth`，均无横向溢出，两个导航入口均可见。
- 真实DeepSeek返回“你猜的是别人会怎么理解，还是你怕自己说不清楚？”，未出现旧前缀、标题或多项操作清单。
- 移动端首次版本的底部操作坞会遮住输入框，审查阶段已改为在文档流中占位的常驻导航，并重新截图确认无重叠。
- 生产首轮回放发现Vercel服务端与浏览器对旧想法时间的格式化时区不同；列表时间固定为`Asia/Shanghai`后重新发布，最终生产控制台为0错误、0警告。
- 本次响应优化前，首页会并发预取6条历史详情，实际点击还出现一条3463毫秒的RSC请求；优化后首页空闲详情预取为0，连续4次不同详情切换分别为330、385、410和738毫秒，每次只有1个RSC请求。首个冷请求仍观察到2508毫秒，保留为真实网络继续验证项，不把它写成已经消失。
- 运行时代码验证基线为提交`f58d2d7`对应的READY部署`dpl_55aokmap64hMigvySx4TgugzJnUn`；桌面侧栏悬停后`clientWidth=scrollWidth=280`、`scrollLeft=0`，320像素移动端`body.scrollWidth=innerWidth=320`。后续文档同步不改变该运行时代码。

## 7. 风险视角扫描

本节为高风险维度摘要，具体问题已在实现阶段修正，最终版本没有遗留issue。

| 视角 | 结论 | 备注 |
|---|---|---|
| 需求对齐 | 无发现 | 信息架构、文案和操作层级均对应M-007验收口径 |
| 正确性与边界 | 无发现 | 检查新建/恢复、同步竞态、旧AI前缀和关系来源过滤 |
| 数据/迁移 | 不适用 | 本轮无DDL、迁移或存量正文改写 |
| 配置/兼容性 | 无发现 | Next.js生产构建通过，320至1440像素真实Chromium验证通过 |
| 安全/权限 | 无发现 | 本轮未改变认证和仓储所有权过滤；API仍沿用服务端会话与userId边界 |

**安全/权限子维度明细**（逐项填写「有发现/无发现/不适用」，有发现的issue写入第5节）：

| 子维度 | 结论 | 关键证据或备注 |
|---|---|---|
| 6A 认证与会话 | 无发现 | 页面和API继续使用现有Supabase Cookie会话，本轮无改动 |
| 6B 授权与数据越权 | 无发现 | 历史、详情、AI和关系路由仍由`requireUser`及仓储`userId`过滤 |
| 6C 输入校验与注入 | 无发现 | 正文继续由现有字段校验与React安全渲染处理，无HTML直出 |
| 6D 敏感数据保护 | 无发现 | 未新增正文日志、凭据文件或浏览器密钥 |
| 6E 业务逻辑安全 | 无发现 | 新建、草稿恢复和后台同步状态已分流；AI重复用户输入会被服务端拒绝 |
| 6F 安全配置与依赖 | 无发现 | 生产构建通过，高危依赖审计为0漏洞 |
| 可观测性/回滚 | 待发布后验证 | 本地控制台和错误状态正常；生产回滚使用上一Git提交 |

## 8. 待补充确认

- 代码侧无待确认项；真实首次用户对产品定位的自然理解需要发布后继续观察。

## 9. 建议后续动作

1. 用至少5名未听过介绍的用户验证5秒入口识别和90秒产品复述，不先解释产品。
2. 重点记录用户是否自然区分“写新想法”“以前的想法”和正文中的“帮我接着想”。
