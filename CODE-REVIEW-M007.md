# retniw 零说明书体验收口 · 代码复审记录

## 当前结论

- 文档状态：发布前复审已完成
- 当前 verdict：`通过（待线上终验）`
- 合入建议：`无P0/P1阻塞，可以进入生产分支提交与部署`
- 说明：本结论覆盖当前工作区代码、自动门禁、生产数据库迁移、生产Auth开关与本地跨端回放，不等同于正式域名验收。Vercel部署、线上动态链路和临时账号清理完成前，不把本轮任务标为完成。

## 本轮评审输入

- 需求：`PRD.md`、`REQUIREMENT-ANALYSIS.md`
- 设计：`TECH-DESIGN.md`
- 任务：`TASK-M-007-零说明书体验收口.md`、`tasks-v23/`
- 数据库：`20260821174500_zero_manual_experience.sql`、`20260822120000_lifecycle_consistency_guards.sql`
- 代码范围：统一浮层、跨端想法管理、单层合集、checkpoint、历史摘要与位置恢复、导出、公开自注册与认证边界
- 评审基线：当前发布候选工作区；准确的发布提交SHA、对比提交和变更文件清单由主控在提交后补写

## 必须复核的问题

| ID | 复核点 | 通过条件 | 当前状态 |
| --- | --- | --- | --- |
| CR-01 | 生命周期并发一致性 | 删除与entry、AI、checkpoint、关系写入并发时，已删除thought不产生新内容；API返回稳定冲突语义 | 通过：第二迁移已在生产应用并读回一致；自动测试通过 |
| CR-02 | 所有权与合集外键 | thought不能归入其他账号合集；匿名和第二账号不能读取或修改合集、停靠点和已删除内容 | 结构与自动测试通过；正式域名双账号回放待完成 |
| CR-03 | 动作幂等与恢复 | move、archive、unarchive、delete、restore重复请求结果稳定；删除可恢复，归档不改变合集 | 自动测试与Chromium乐观动作通过；合集动态完整链路待完成 |
| CR-04 | 浮层与焦点 | 任意菜单/抽屉单开；空白、Escape、打开其他层和完成操作均关闭；焦点返回有效触发器 | Chromium通过：关闭、互斥、焦点与键盘路径均已回放 |
| CR-05 | 跨端操作一致 | 桌面更多/右键与移动左滑/长按执行相同语义；滚动不误触；同一时刻只展开一个滑动条目 | Chromium通过：右键、触摸滑动、长按与单开均已回放；登录后WebKit主链待完成 |
| CR-06 | 停靠与位置恢复 | checkpoint重试复用请求ID；“先到这里”不调用AI、不归档，移动端回到全部想法；较新的手动位置优先 | 自动测试通过；checkpoint动态链路待完成 |
| CR-07 | 删除内容的可达性 | 已删除列表不产生详情死链，恢复后可重新打开；关联系统不返回已删除对端 | 自动测试与Chromium删除、恢复乐观动作通过 |
| CR-08 | 导出顺序与语义 | Markdown按时间交错输出entry与checkpoint；结构化导出保留合集、归档、删除和停靠状态 | 自动测试通过；正式域名导出回放待完成 |
| CR-09 | 公开自注册 | 无效输入不调用Supabase；注册拿到session直接进入；错误不枚举邮箱；Cookie会话、退出重登正常 | 自动测试通过；生产开关已读回；正式域名注册、重登与双账号隔离待完成 |
| CR-10 | 响应与加载连续性 | 点击100毫秒内有就地状态；不批量预取详情；页面壳不被整块骨架替换；长内容摘要不扫描全文 | Chromium加载竞态与乐观反馈通过；正式域名耗时待记录 |

## 交付门禁

- [x] `git diff --check`通过，发布候选范围完成检查。
- [x] `npm test -- --run`通过：19个测试文件、117项测试。
- [x] `npm run typecheck`通过。
- [x] `npm run lint`通过。
- [x] `npm run build`通过。
- [x] `npm audit --omit=dev --audit-level=high`通过，无高危问题。
- [x] 两个生产迁移均已应用并读回一致；第二迁移的复合owner外键、三个触发器与两个函数存在。
- [ ] 跨端终验：Chromium已覆盖320、375、768、1024、1440像素且无横向溢出；WebKit只完成登录页基线与提交反馈，登录后主链待完成。
- [ ] 正式域名完成注册、写下、接着想、先到这里、回到全部想法、再次继续、移入、归档、删除、恢复和导出闭环。
- [ ] 生产Supabase已读回`Allow new users = ON`、`Confirm email = OFF`；临时账号注册、双账号隔离与清理待完成。

## 证据记录

- 自动门禁：19个测试文件、117项测试，typecheck、lint、build、依赖高危审计和diff检查全部通过。
- 生产数据库：`20260822120000_lifecycle_consistency_guards.sql`已应用；本地与远端迁移清单一致，约束、触发器和函数读回一致。
- 生产Auth：Dashboard已读回公开注册开启、邮箱确认关闭；未配置邀请码或管理员建号入口。
- Chromium本地回放：320、375、768、1024、1440像素无水平溢出；菜单关闭、互斥、焦点、右键、触摸滑动、长按、加载竞态和乐观动作已验证。
- WebKit本地回放：只覆盖375和1440像素登录页基线及提交反馈，不外推到登录后主链。
- 待主控补写：发布提交SHA、Vercel部署ID、正式域名Network与控制台记录，以及临时账号清理证据。

## 仍需真实用户验证

- 至少5名未听过介绍的用户中，至少4名能在90秒内将retniw理解为记录、承接或继续自己的想法，而不是聊天机器人。
- 用户能否自然区分“移入、归档、删除”和“先到这里”，以及长想法停靠后是否愿意回来继续。
- 新体验者能否只凭正式网址完成创建账号、开始记录和再次登录，不向项目所有者询问密码或操作说明。
