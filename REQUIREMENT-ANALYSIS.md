# retniw 需求分析

## 产品定义

retniw 承接尚未成形的想法，但不要求用户把全部上下文迁入其中，也不把内容锁在其中。

它同时支持两种自然状态：一句话可以独立停留，也可以继续发展成一段连续思考。外部内容可以进入当前思考，碎片、完整过程和关系数据都可以离开 retniw。

用户不需要先了解产品说明。页面用一条可直接走通的链路表达产品：先记下来，慢慢表达，之后回来看看它们之间有什么联系。导航先承担“写新想法”和“以前的想法”，当前想法就是正在阅读和输入的正文；“回看”属于以前内容的下一步，不与记录入口平铺。AI 不承担产品入口和主要叙事。

一段思考不需要被判定为完成。用户可以选择“先到这里”，留下这一次停下的位置，再回到全部想法；以后仍能从这里继续。归档、合集归属和删除分别处理可见性、内容组织和数据管理，不与思考是否结束混为一个状态。

## 用户角色

- 个人用户：随手写下一句话，也会在同一个想法中持续追加、回看和整理。
- 小规模内测用户：第一次打开时不了解产品，需要仅凭页面结构完成记录、切换、停靠和管理。
- 新体验者：只拿到正式网址，能够自行创建独立账号，不需要接收项目所有者发来的初始密码。

## 核心问题

- 念头出现时，命名、分类和整理会打断捕捉。
- 单条碎片不足以承载用户被问题触发后继续思考的需求。
- 强制把连续思考切成多个条目，会破坏上下文并增加使用负担。
- 封闭的输入输出会迫使用户把 retniw 当作唯一上下文来源，形成新的信息孤岛。
- 保存、跳转、模型处理和关系检查缺少统一状态，会让页面变化显得生硬且不可预期。
- 当前页面没有可见的“新想法”和“所有想法”，用户只能把返回箭头误当成切换入口。
- 首屏最明确的操作是推进、追问、整理和寻找联系，产品因此容易被理解成大模型或聊天工具。
- `vercel.app` 在部分中国大陆网络无法直接访问，当前地址不能承担低摩擦内测入口。
- 长想法没有自然停靠点，用户只能无休止地继续写或直接离开，不知道这一次思考落在了哪里。
- 归档同时被用来表达“移出常用列表”和“放入更大的内容分类”，生命周期与内容归属混在一起。
- 历史条目缺少归档、移入和删除；移动端和桌面端也没有符合各自使用习惯的快捷入口。
- 多个菜单可以同时展开，点击空白后仍不关闭；详情切换又会替换整块工作区，放大等待感。
- 每增加一名体验者都由项目所有者人工建号和发密码，会把自然进入产品变成人工邀请流程。
- 当前 AI 只读取正在打开的一个想法；用户即使要求“整理以前的想法”，也只能得到单想法回答，已有内容仍是彼此隔离的。
- “已删除”被做成与“全部、归档”并列的内容区，删除和归档都被当作列表筛选，既增加层级，也没有体现两者完全不同的用途。

### 内测信号（2026-08-24）

1名内测用户在写手帐时主动把retniw与自己的记录习惯联系起来。原话：

> 我理解你那个网站了
>
> 我今天写手帐的时候突然就觉得，先记录 后表达
>
> 我记手帐也是很多想法
>
> 但是我到现在都还不是很会完全表达自己，所以记录的路还很长
>
> 感觉你就想解决这样的问题

这说明对这名用户而言，产品已经从抽象说明变成了与自身场景相关的用途：“先记下来，再慢慢表达。”这是当前方向有效的积极信号。它来自1名用户，不能据此判断多数人都已理解产品，也没有直接验证跨想法主动复盘；下一步需要验证的是，当记录变多时，retniw能否帮助用户回看并发现其中的联系。

## 产品结构

### 记录、联系与回看

产品只保留三层，每一层的事实归属不同。

- 记录：用户写下或导入的原文，是唯一事实源。AI 输出不反过来成为用户事实。
- 联系：系统依据两端原文提出候选；只有用户选择“保留”后，才成为长期联系。选择“忽略”后不再反复提示同一对内容。
- 回看：把待确认和已保留的联系集中呈现，帮助用户重新看见自己写过的内容。它是以前内容的独立视图，不是一段 AI 对话，也不写回某个想法。

单个想法内的 AI 与跨想法回看使用两条清楚的边界：继续和整理仍由用户当下主动调用；跨想法比较默认关闭，用户第一次进入“回看”时明确开启，也可以随时关闭。开启后，保存新原文只负责触发后台比较，记录成功不等待模型结果。

### 内容单元

内容单元保存一段不可被覆盖的原始内容，并标明来自用户、AI 或外部导入。只有一段内容时，它本身就是完整记录；后续也可以继续追加。

### 连续思考

一组有顺序的内容单元构成一段连续思考。它可以只有一句话，也可以不断追加，开始记录前不需要选择内容形态。

用户可以自行追加内容，也可以主动选择“帮我接着想”或整理。前者由 AI 根据当前用户内容选择问一个具体问题或指出一个角度，不要求用户理解“推进”和“追问”的区别。AI 输出按发生顺序保留；完整过程默认保存，用户可以继续、离开、回来或导出。普通输入和导入不自动生成 AI 正文；只有用户明确开启跨想法回看后，保存新原文才会在后台触发一次有界比较。

### 工作区导航

全局只提供两个导航动作，并把当前内容留在主区。

- “新想法”随时可见，进入空白输入状态，不由返回箭头或品牌标识代替。
- “以前的想法”随时可见，用第一段原文摘录展示已有内容，不新增标题要求。
- “当前想法”保留原文、追加内容、导入内容和 AI 产出，并允许继续输入。
- 返回只表示回到浏览历史，不再承担新建功能。
- 退出收进账号入口，不占据主要导航位置。

桌面端常驻展示“写新想法”和已有想法列表；移动端固定显示“写新想法”“以前的想法”两个动作，已有想法通过面板展开，不再排在长内容末尾。

### 停靠、归档与删除

三者是独立行为。

- “先到这里”：结束这一次思考，可选留一句当前判断或下次入口，然后返回全部想法；不自动归档，也不要求 AI 生成结论。
- “归档”：把想法移出常用列表，原文、合集归属和关系完整保留，可以恢复。
- “删除”：强确认后永久删除完整想法及其关联内容，不提供恢复或“已删除”区域。左滑、长按和右键都只能先进入同一个确认层，不能直接执行。

历史根视图先展示最近想法和合集，再以次级入口进入“归档”。归档子视图使用返回箭头和单一标题，不再显示“全部 / 归档”并列标签。旧版本已经软删除的数据本轮继续隐藏，不在界面恢复，也不随发布批量清理。

再次打开有停靠点的想法时，先回到上次停下的位置和继续输入入口，不把用户扔回长内容顶部。用户写下新内容后，原停靠点仍作为过程边界保留。

### 合集

合集是单层内容容器，不是文件夹树、标签或另一个想法。

- 一个合集可以包含多个想法；一个想法最多移入一个合集，也可以不属于任何合集。
- 用户写下内容时不要求选择合集；只在之后需要整理时使用“移入”。
- “移入”可以选择已有合集，也可以就地新建合集。
- 删除合集不删除其中的想法，只解除归属。
- 想法之间的跨主题关系继续由“联系”承担，不用多个合集重复表达。

桌面侧栏和移动端“以前的想法”面板使用同一结构：先展示最近想法，再展示合集入口；归档内容作为次级入口，不与最近列表平铺。打开合集后只展示其中的想法，仍可直接另起想法或回到全部内容。

### 历史条目操作

- 点击或轻触条目：打开想法。
- 桌面端：悬停或键盘聚焦时显示更多入口，右键打开同一组操作。
- 移动端：左滑露出“归档”和“删除”，长按打开包含“移入、归档、删除”的操作抽屉。
- “移入、归档、删除”使用简短文字并配套线性 SVG；图标辅助识别，不替代文字。
- 打开一个菜单或抽屉时关闭其他同类浮层；点击空白、按 Escape 或完成选择后收起当前浮层。
- 删除是危险操作，与移入、归档分组展示，不用颜色和位置把三者伪装成同级快捷动作。

### 关系

关系连接内容单元或连续过程。系统只能依据两端用户原文或导入原文提出候选，用户确认后才成为长期关系。关系不改变原文，也不要求用户维护图谱。

- 跨想法回看默认关闭。首次进入时先说明：开启后，保存新内容会把当前内容和必要的旧想法交给 DeepSeek 比较；系统只提出可能的联系，不改写内容，也不自动保留。
- 用户开启后，偏好跨设备生效并可随时关闭。关闭只停止后续自动比较，不删除原文、已保留联系或仍待处理的候选。
- 每次保存只在响应完成后异步检查，失败不影响保存、继续输入或离开。比较只读取当前账号下必要且有界的用户原文和导入原文，归档内容可以参与，已删除内容不能参与。
- 候选必须同时展示两端原文依据和一句直接理由。系统不根据候选自动续写、分类、归档或形成最终判断。
- 待确认、已保留和已忽略是联系的决定状态，不改变记录本身。

### 回看

“回看”位于“以前的想法”之下，打开独立主区。

- 未开启时，只展示简短的数据范围说明和“开启”动作，不用理念文案说服用户。
- 已开启但没有候选时，保持安静，不生成泛泛总结，也不为了填满页面制造联系。
- 有候选时，按最近产生顺序展示两端原文、所属想法和理由，主动作是“保留”，次动作是“忽略”。
- 已保留联系继续可见，成为用户以后回来的线索；不把它们改写成 AI 文章或新想法。
- 关闭入口与开启状态在同一页面可见，用户不需要进入账号设置寻找。

### 开放边界

导入和导出是内容边界能力，不另建内容体系。

- 输入：直接输入、粘贴外部文字、导入 Markdown 或纯文本。
- 输出：复制选中内容、导出完整过程为 Markdown、导出内容与关系为结构化数据。
- 所有导入内容保留来源说明；所有导出内容包含稳定标识、时间、作者类型和关系，避免只能在 retniw 内解释。

### 账号入口

- 登录页同时提供“登录”和“创建账号”。新体验者用邮箱和自设密码注册，无邀请码，不由项目所有者代建账号。
- 生产环境允许邮箱密码注册并关闭邮箱确认，注册成功后直接建立会话；确认邮件不成为进入产品的前置步骤。
- 公开的是注册入口，不是用户内容。想法、合集、停靠点、关系和导出继续只对当前账号可见。
- 本轮不增加邀请后台、邮箱白名单或自有SMTP。未来若确需验证邮箱或找回密码，再先补可靠邮件投递。

## 核心场景

### 捕捉后离开

用户输入一句话，内容立即保存。用户可以直接离开，不需要回应 AI，也不需要完成后续步骤。

### 捕捉后继续

内容保存后，用户可以直接继续写，也可以调用 AI 提问或回应。一次 AI 输出或关系检查不会结束当前思考。

### 引入外部上下文

用户把其他应用中的文字粘贴或导入当前过程。retniw 将其作为带来源的上下文使用，不要求用户重写成碎片。

### 带走结果

用户可以复制一段内容、导出完整过程，或导出内容与关系数据。离开 retniw 后仍能理解内容的顺序和关系。

### 重新相遇

用户第一次进入“回看”时选择是否开启跨想法比较。开启后，系统在新原文保存完成后异步检查用户写下或导入的内容；发现有依据的候选时放入“回看”，用户可以保留或忽略。检查和候选不阻断当前思考，也不自动改写用户观点。

### 另起一个想法

用户在任何一段连续思考中，都能直接开始一个空白想法，也能打开已有想法。未同步完成的内容继续保存在本地，新建和切换不等待同步。

### 先到这里

长想法写到当前阶段后，用户选择“先到这里”。页面允许留一句话，也允许直接返回全部想法；动作立即完成，不等待 AI。下次打开时从停靠点继续。

### 整理以前的想法

用户可以把想法移入一个单层合集、归档或删除。移动端使用左滑和长按，桌面端使用更多入口和右键；所有显式入口执行同一组数据操作。

### 回看以前的想法

用户从“以前的想法”进入“回看”，先处理系统找到的候选，再看到自己已经保留的联系。页面不要求用户和 AI 对话，也不把“回看”限制在刚刚打开的一个想法中。

## 全链路交互

```text
创建账号或登录
  ↓
输入或导入上下文
  ↓
本地立即呈现并保存原文
  ↓
后台同步，当前页面保持稳定
  ↓
用户继续写，或按需调用单想法 AI
  ↓
用户继续输入、另起想法、切换已有想法、离开或导出
  ↓
需要停下时选择“先到这里”，返回全部想法
  ↓
按需移入合集、归档或删除
  ↓
用户明确开启跨想法回看后，新原文在保存响应完成后异步比较
  ↓
有依据的候选进入“回看”，用户保留或忽略
```

- 保存不触发整页跳转；当前输入区域原地进入连续记录状态。
- 页面先确认原文已保存；单想法 AI 只有用户主动调用时才展示正在处理和逐步返回的内容。
- 原文保存、单想法 AI 和后台联系检查互不阻塞，不共享一个总 loading；后台检查不让输入区进入等待状态。
- 返回、刷新和跨端打开均恢复同一思考位置。
- AI 输出后输入区域仍然可用，用户可以继续追加内容。
- “先到这里”只记录停靠点，不自动归档；归档不改变合集归属，移入合集也不表示思考结束。
- “回看”只承接联系候选和已保留联系，不替用户总结全部内容。

## 首次使用

- 不使用教程弹窗、示例对话或功能说明页解释产品。
- 空白页只说“写下你正在想的。”，输入提示为“写在这里”。
- 已有内容后只使用“继续写”和“接着写”，不再增加解释性铺垫。
- “写新想法”是持续可见的主导航动作，不依赖一次性教程提示。
- 没有内容时不展示 AI 操作；有内容后，正文旁只出现“帮我接着想”，整理收进更多操作；“回看”留在“以前的想法”中。
- AI 产出以“一个可继续的方向”或“整理后的内容”作为正文旁支，不使用聊天气泡和轮次作为页面结构，也不显示“可以继续写”等指令式前缀。
- “回看”第一次打开时只说明处理范围和开关，不预放示例联系，不把开启包装成新手必做步骤。

## 中国大陆内测入口

大陆内测只使用自定义域名`retniw.cn`。旧`vercel.app`仅保留Vercel边缘HTTP 308；大陆公共DNS可能被污染，不再作为可用入口。

内测采用两步验证：

1. 使用自定义域名连接当前 Vercel 部署，在中国移动、联通和电信网络下验证直接访问、登录、保存和 AI 操作。
2. 任一网络仍需代理或频繁失败时，将同一域名切换到中国香港托管的同版本应用，Vercel 保留为备用。

浏览器仍只访问 retniw 域名；Supabase 和 DeepSeek 继续由 Next.js 服务端调用。首轮不迁移数据库，不自建 Supabase，也不引入大陆 CDN。

中国香港托管仍属于跨境访问，必须以真实网络验收，不能预先承诺所有地区和运营商稳定。面向大陆长期公开运行时，再单独评估中国大陆托管、ICP备案、数据与鉴权迁移，不纳入本轮内测。

## 首版范围

- 登录页支持邮箱和密码公开自注册，无邀请码；生产关闭邮箱确认，注册后直接进入。
- 一句话捕捉与连续思考使用同一入口。
- 原文、用户后续输入、按需产生的 AI 输出和导入内容按顺序保留。
- 直接粘贴外部文字，导入 Markdown 和纯文本。
- 复制单段内容，导出完整过程为 Markdown。
- 导出全部内容和已确认关系为结构化数据。
- 跨想法回看默认关闭；用户明确开启后，新原文保存完成会异步产生少量候选，结果仍由用户确认。
- 本地即时反馈、后台同步、分阶段 loading 和 AI 流式回应。
- 手机与 Mac 访问同一内容和思考进度。
- “写新想法”和“以前的想法”在手机与 Mac 上都有固定入口，当前想法直接在主区继续。
- 使用自定义域名完成大陆三网内测；Vercel 不满足时切换到中国香港托管。
- 长想法可以用“先到这里”留下停靠点并回到全部想法，不自动归档。
- 单层合集支持就地新建和“移入”；一个想法最多属于一个合集。
- 历史条目支持移入、归档和删除；移动端支持左滑与长按，桌面端支持更多入口与右键。
- 归档作为“以前的想法”的子视图；不提供“已删除”视图，新删除在强确认后永久生效。
- “回看”集中展示待确认和已保留的跨想法联系，并提供可见的开启与关闭入口。
- 菜单、抽屉和对话框遵循单开、点击空白关闭和 Escape 关闭。
- 详情切换保留工作区外壳，只在内容区显示就地状态；历史摘要不随长内容增长而重复加载完整正文。

## 非目标

- 首版不接入 Notion、Obsidian、微信等第三方平台 API。
- 不解析图片、音频、PDF 或复杂富文本文件。
- 不做团队协作、公开发布或社交互动。
- 不做邀请码、邀请后台、邮箱白名单、自有SMTP或邮件链接登录。
- 不做多层文件夹、子项目、标签体系或可视化图谱；合集只保留一层。
- 不自动把思考过程改写成文章、任务或最终观点。
- 不用向量库或独立图数据库支撑首版关系能力。
- 不生成全量 AI 周报、人格结论、主题画像或自动项目分类；回看只呈现有原文依据的联系。
- 不在记录前要求用户选择合集、标题或分类；不自动给想法分类。
- 不把新手说明、示例聊天或自动 AI 回复作为定位表达。
- 本轮不建设中国大陆正式生产环境，不处理 ICP 备案和境内数据库迁移。

## 体验原则

- 输入可以短，也可以连续，不预先要求用户选择形态。
- 原文先出现，网络和 AI 不阻塞表达。
- 当前页面保持稳定，状态变化在原位置发生。
- 单想法 AI 由用户主动调用，输出作为当前内容的一部分按顺序展示；跨想法比较只有用户明确开启后才在保存完成后运行。
- 先让用户理解内容和去向，再展示 AI 能力。
- 页面按内容顺序和来源组织信息。
- 外部上下文进得来，原始内容、连续过程和关系出得去。
- 不为开放性提前建设第三方集成平台；先保证通用文件和结构化数据可迁移。
- 新建、切换和继续是三个独立动作，不能再借用返回或品牌链接表达。
- 停靠、归档、合集归属和删除分别表达过程边界、可见状态、内容归属和数据管理，不能复用一个按钮或状态。
- 记录、联系和回看分别表达事实、候选判断和重新理解，不能把 AI 候选写回用户原文。
- 文案使用“移入、归档、删除”等自然动词；SVG 只帮助扫视，不用装饰效果替代产品语言。
- 隐藏手势只提供快捷操作，所有能力都必须有可发现的显式入口。

## 验收判断

- 新体验者只拿到正式网址即可自行创建账号，不需要项目所有者建号或传密码；注册后直接进入空白工作区。
- 公开注册不改变数据私有性；两个独立账号之间的想法、合集、停靠点、关系和导出均不可互读。
- 用户输入一句话后可以直接离开，也可以在原页面继续追加内容。
- 用户不调用 AI 时也能完成捕捉、继续、导入和导出。
- 连续思考不会因一次 AI 输出或关系检查被迫返回首页。
- 保存后页面不整页跳转，并能区分本地保存、同步和 AI 处理状态。
- 用户可以把一段外部文字带入当前过程，并看到来源。
- 用户可以导出完整过程，离开 retniw 后仍能理解内容顺序。
- 用户可以导出内容与已确认关系，数据不依赖 retniw 页面才能解释。
- AI 或关系检查失败时，已输入内容和当前思考仍可继续。
- 未开启跨想法回看时，保存新内容不会把历史想法交给 DeepSeek；开启和关闭在手机与 Mac 上保持一致。
- 开启后保存新内容先返回成功，再异步比较必要的当前与历史原文；模型失败、超时或没有候选都不改变保存结果。
- 每个联系候选都能看到两端原文依据，用户可以保留或忽略；没有依据时页面保持空，不生成泛泛总结。
- 归档想法可以参与回看，已删除想法不能参与；永久删除后相关内容与联系不可恢复。
- 未听过产品介绍的用户能在 5 秒内找到新想法和已有想法入口。
- 至少 5 名首次用户独立体验 90 秒后，至少 4 人将产品描述为记录、承接或继续想法，而不是 AI 聊天。
- 首次用户不会认为保存内容后必须等待 AI 回复，也能在不使用 AI 的情况下完成记录和回看。
- 大陆内测账号在不使用代理的情况下，分别通过移动、联通和电信网络完成打开、登录、保存和一次 AI 操作；失败时如实记录网络与步骤，不以单一设备成功代替三网验收。
- 长想法选择“先到这里”后立即返回全部想法；下次打开恢复至停靠点，且该动作不自动归档。
- 一个想法最多移入一个合集；删除合集后想法仍存在，归档后合集归属仍保留。
- 历史根视图不再使用“全部 / 归档 / 已删除”并列标签；归档通过次级入口进入，返回后仍回到以前的想法。
- 移动端左滑可见归档和删除，长按可见移入、归档和删除；桌面端更多入口与右键提供同一组操作。
- 打开一个浮层后再打开另一个，前一个自动关闭；点击空白或按 Escape 后浮层收起。
- 点击历史条目后 100 毫秒内出现就地状态；加载过程中侧栏和已显示内容不被整块骨架替换。

## 当前默认

- 账号入口公开自注册：邮箱加自设密码，无邀请码；生产`Allow new users`开启、`Confirm email`关闭。
- 完整连续过程默认长期保留并可导出；用户不需要逐段选择保存。
- 首版开放输入使用粘贴、Markdown 和纯文本，开放输出使用复制、Markdown 和结构化数据。
- 单想法 AI 只在用户主动选择操作时触发；跨想法比较默认关闭，明确开启后才随新原文异步触发。
- 首页继续承担打开即写；工作区固定提供“写新想法”和“以前的想法”，详情页不再依赖返回箭头切换。
- 大陆内测先验证自定义域名，失败即切中国香港托管；正式境内部署另行立项。
- “先到这里”与归档解耦：前者留下过程边界，后者只改变常用列表中的可见性。
- 合集为单层一对多结构，一个想法最多属于一个合集；跨主题连接继续使用“联系”。
- 条目动作固定使用“移入、归档、删除”，图标只作辅助识别。
- 删除使用强确认后永久执行，不提供恢复或“已删除”列表；旧版软删除内容继续隐藏，历史清理另行确认。
- “回看”是以前内容的次级视图，只呈现候选与已保留联系，不成为第三个同级记录入口。

## 风险与默认处理

- 关闭邮箱确认意味着当前不验证邮箱归属，也不能依赖内置邮件完成稳定找回；本轮优先消除内测建号门槛，后续若开放范围扩大，再接正式SMTP并补邮箱验证与找回链路。
- 左滑、长按和右键存在发现成本，因此必须同时保留可见的更多入口。
- 删除涉及完整过程和关系，确认层必须明确“删除后无法恢复”；确认后直接永久删除。旧版软删除内容本轮不批量清理。
- 合集数量增长后可能需要搜索或排序，本轮先验证单层结构，不提前增加筛选系统。
- 自动比较会增加模型调用和历史内容处理范围，因此默认关闭、限制候选数量与输入长度、只在保存响应之后运行，并提供随时关闭入口。
- 系统可能错过联系或提出不成立的联系；没有候选不是错误，候选也不能替用户形成结论，仍需真实用户验证打扰度、理由可信度和保留率。
