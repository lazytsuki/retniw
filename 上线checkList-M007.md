# retniw 零说明书体验收口 · 上线checkList

> 当前状态：发布前检查通过，待提交、部署和正式域名终验。勾选只记录本轮真实证据；上一版`f58d2d7`的验证结果不能替代本轮线上回放。

## 1. 发布前冻结

- [ ] 记录本轮发布提交SHA、上一生产提交SHA、目标GitHub分支和Vercel Production Branch。
- [ ] 确认工作区只包含本轮代码、迁移、测试与文档；不包含`.env*`、账号、密码、token或临时调试文件。
- [x] `git diff --check`通过。
- [x] `npm test -- --run`通过：19个测试文件、117项测试；`npm run typecheck`、`npm run lint`、`npm run build`通过。
- [x] `npm audit --omit=dev --audit-level=high`通过，无高危问题。
- [x] `CODE-REVIEW-M007.md`发布前verdict为“通过（待线上终验）”。

## 2. 数据库

- [x] 2026-08-21已执行`20260821174500_zero_manual_experience.sql`并幂等回填17条旧摘要；本轮不重复破坏性执行。
- [x] `20260822120000_lifecycle_consistency_guards.sql`已在生产应用；本地与远端迁移清单一致，复合owner外键、三个写入锁触发器和两个函数已读回。
- [ ] 第二账号不能查看或修改第一账号的合集、想法、停靠点、归档和已删除内容。
- [x] 自动测试与生产数据库一致性验证覆盖删除和后续写入冲突；正式域名错误表现仍随主链终验复核。
- [ ] 重复执行摘要回填结果不变，不修改entries正文、关系或活动时间。
- [ ] 记录数据库回退边界：应用可回退到上一提交；新增可空列、表、约束和触发器暂不删除，结构清理由单独不可逆变更处理。

## 3. Supabase Auth

- [x] 生产`Allow new users`读回为`ON`。
- [x] 生产`Confirm email`读回为`OFF`。
- [x] 不配置邀请码、管理员建号接口或仓库内SMTP凭据。
- [ ] 正式域名创建临时账号后直接进入首页；退出后可用同一凭据重新登录。
- [ ] 注册失败使用通用提示，不向未登录用户区分邮箱是否存在。
- [ ] 用另一临时账号验证跨账号404；验证后清理临时账号和数据，不在发布记录中写邮箱或密码。

## 4. 部署顺序

1. 完成第1、2节，并读回第3节的两个生产Auth开关；数据库一致性迁移先于新应用流量。
2. 推送当前生产分支，等待Vercel部署状态为READY。
3. 确认`https://retniw.cn`指向本轮提交；确认`https://retniw.vercel.app`仍以HTTP 308跳转到新域名。
4. 执行第3节剩余账号回放及第5至7节线上回放。任一P0失败时停止扩大测试并回滚应用。

## 5. 核心产品闭环

- [ ] 空白页能直接写，且没有自动AI回复或聊天式首屏。
- [ ] 走通“写下→接着想→先到这里→回到全部想法→再次继续”。
- [ ] “先到这里”有备注和无备注均可；不调用DeepSeek、不自动归档、不改变合集。
- [ ] 走通“移入、归档、删除、恢复”；一个想法最多一个合集，删除合集不删除想法。
- [ ] 已删除列表不打开404死链；恢复后可正常进入详情。
- [ ] Markdown和结构化导出保留内容顺序、checkpoint、合集和状态语义。

## 6. 跨端与交互

- [x] Chromium在320、375、768、1024、1440像素无横向溢出；桌面侧栏选中边框完整。
- [ ] WebKit已覆盖375和1440像素登录页基线及提交反馈；登录后主链与控制台终验待完成。
- [x] Chromium桌面更多入口和右键可用；移动左滑、长按可用。
- [x] Chromium同一时刻只有一个菜单、抽屉或滑开条目；空白、Escape、打开其他层和完成操作后收起，焦点返回合理位置。
- [ ] checkpoint后移动端进入全部想法，桌面端回到自然的列表状态。

## 7. 响应与运行时

- [ ] 新建、历史切换和管理动作在100毫秒内出现就地状态。
- [ ] 首页空闲不批量预取历史详情；每次历史切换只有一次对应RSC请求。
- [x] Chromium慢响应竞态下保留工作区外壳，以“正在加载”反馈；较慢旧请求不会覆盖较新的视图。
- [ ] 记录冷请求与连续请求的真实耗时，不把首个慢请求写成已消失。
- [ ] 生产控制台0错误、0警告；失败状态不会丢失已经写下的内容。

## 8. 回滚与观察

- [ ] 记录上一READY部署及回滚提交；应用回滚后重新验证登录、保存、旧域名跳转和旧数据读取。
- [ ] 上线后观察注册失败、登录失败、API 409/500、AI首段时间和Vercel错误日志；日志不得包含正文、邮箱、密码或密钥。
- [ ] 记录仍需真实用户验证的三项：首次定位理解、四个动作的自然区分、无需说明完成自注册与再次登录。
